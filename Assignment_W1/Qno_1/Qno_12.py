#curency comvertor in(usd_pkr)

exchange_rate = 278.5
usd_amount = float(input("Enter amount in USD: "))
pkr_amount = usd_amount * exchange_rate

pkr_amount = usd_amount * exchange_rate
print("Equivalent amount in PKR:", pkr_amount)
